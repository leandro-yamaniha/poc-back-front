<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.4</version>
        <relativePath/>
    </parent>

    <groupId>com.beautysalon</groupId>
    <artifactId>beauty-salon-reactive</artifactId>
    <version>1.0.0</version>
    <name>Beauty Salon Reactive Backend</name>
    <description>Reactive Beauty Salon Management System with Spring Boot WebFlux and Undertow</description>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <spring-boot.version>3.5.4</spring-boot.version>
        <cassandra-driver.version>4.18.1</cassandra-driver.version>
        <reactor-test.version>3.6.10</reactor-test.version>
        <graalvm.version>23.1.1</graalvm.version>
        <native-buildtools.version>0.11.2</native-buildtools.version>
    </properties>

    <dependencies>
        <!-- Spring Boot WebFlux for Reactive Programming -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-starter-undertow</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.hibernate.validator</groupId>
                    <artifactId>hibernate-validator</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        
        <!-- Use Netty instead of Undertow -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-reactor-netty</artifactId>
        </dependency>

        <!-- Spring Data Cassandra Reactive (without JCTools) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-cassandra-reactive</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>org.jctools</groupId>
                    <artifactId>jctools-core</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.hibernate.validator</groupId>
                    <artifactId>hibernate-validator</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- JCTools Native Image Compatible Version - versão reportada como mais estável com GraalVM -->
        <dependency>
            <groupId>org.jctools</groupId>
            <artifactId>jctools-core</artifactId>
            <version>3.3.0</version>
        </dependency>
        
        <!-- Forçar versão específica do Netty reportada como mais estável com GraalVM -->
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-all</artifactId>
            <version>4.1.92.Final</version>
        </dependency>

        <!-- Netty Native Transport for Linux (ARM64) -->
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-transport-native-epoll</artifactId>
            <classifier>linux-aarch_64</classifier>
        </dependency>

        <!-- Netty Native Transport for macOS (development) -->
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-transport-native-kqueue</artifactId>
            <classifier>osx-aarch_64</classifier>
            <scope>provided</scope>
        </dependency>

        <!-- Actuator for Health Checks -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Jackson for JSON Processing -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

        <!-- Reactor Core -->
        <dependency>
            <groupId>io.projectreactor</groupId>
            <artifactId>reactor-core</artifactId>
        </dependency>

        <!-- Reactive Streams -->
        <dependency>
            <groupId>org.reactivestreams</groupId>
            <artifactId>reactive-streams</artifactId>
        </dependency>

        <!-- SpringDoc OpenAPI for WebFlux -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webflux-ui</artifactId>
            <version>2.8.11</version>
            <exclusions>
                <exclusion>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-starter-validation</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- GraalVM SDK for Native Hints -->
        <dependency>
            <groupId>org.graalvm.sdk</groupId>
            <artifactId>graal-sdk</artifactId>
            <version>24.1.1</version>
            <scope>provided</scope>
        </dependency>

        <!-- Test Dependencies -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.projectreactor</groupId>
            <artifactId>reactor-test</artifactId>
            <version>${reactor-test.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>cassandra</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <extensions>
            <!-- OS Maven Plugin for Netty Native Transport -->
            <extension>
                <groupId>kr.motd.maven</groupId>
                <artifactId>os-maven-plugin</artifactId>
                <version>1.7.1</version>
            </extension>
        </extensions>
        
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.beautysalon.reactive.BeautySalonReactiveApplication</mainClass>
                    <jvmArguments>
                        --enable-preview
                    </jvmArguments>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                    <image>
                        <builder>paketobuildpacks/builder:tiny</builder>
                        <env>
                            <BP_NATIVE_IMAGE>true</BP_NATIVE_IMAGE>
                        </env>
                    </image>
                </configuration>
                <executions>
                    <execution>
                        <id>process-aot</id>
                        <goals>
                            <goal>process-aot</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <source>21</source>
                    <target>21</target>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.0</version>
            </plugin>

            <!-- JaCoCo for Test Coverage -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.12</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            
            
            <!-- GraalVM Native Build Tools with AOT -->
            <plugin>
                <groupId>org.graalvm.buildtools</groupId>
                <artifactId>native-maven-plugin</artifactId>
                <version>${native-buildtools.version}</version>
                <extensions>true</extensions>
                <configuration>
                    <classesDirectory>${project.build.outputDirectory}</classesDirectory>
                    <metadataRepository>
                        <enabled>false</enabled>
                    </metadataRepository>
                    <fallback>false</fallback>
                    <verbose>true</verbose>
                    <buildArgs>
                        <!-- Core native image options -->
                        <buildArg>--no-fallback</buildArg>
                        <buildArg>--install-exit-handlers</buildArg>
                        <buildArg>--enable-url-protocols=http,https</buildArg>
                        
                        <!-- Debugging and reporting -->
                        <buildArg>-H:+ReportExceptionStackTraces</buildArg>
                        <buildArg>-H:+AddAllCharsets</buildArg>
                        
                        <!-- Spring Boot specific -->
                        <buildArg>--initialize-at-run-time=io.netty</buildArg>
                        <buildArg>--initialize-at-run-time=reactor.core.scheduler</buildArg>
                        <buildArg>--initialize-at-run-time=reactor.netty</buildArg>
                        
                        <!-- Logging -->
                        <buildArg>--initialize-at-run-time=ch.qos.logback</buildArg>
                        <buildArg>--initialize-at-run-time=org.slf4j</buildArg>
                        
                        <!-- JCTools Specific Flags -->
                        <buildArg>--initialize-at-run-time=io.netty.util.internal.shaded.org.jctools</buildArg>
                        <buildArg>--allow-incomplete-classpath</buildArg>
                        <buildArg>--report-unsupported-elements-at-runtime</buildArg>
                        <buildArg>-H:ReflectionConfigurationFiles=${project.basedir}/src/main/resources/META-INF/native-image/reflect-config.json</buildArg>
                        
                        <!-- Cassandra specific - Forçar inicialização em runtime -->
                        <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.os</buildArg>
                        <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.util.concurrent</buildArg>
                        <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.metrics</buildArg>
                        <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.session</buildArg>
                        <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.channel</buildArg>
                        <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.connection</buildArg>
                        <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.pool</buildArg>
                        <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.cql</buildArg>
                        <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.metadata</buildArg>
                        <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.loadbalancing</buildArg>
                        <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.control</buildArg>
                        <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.type</buildArg>
                        <buildArg>--initialize-at-run-time=com.datastax.oss.driver.api.core</buildArg>
                        <buildArg>--initialize-at-run-time=com.datastax.dse</buildArg>
                        
                        <!-- More specific Netty configurations -->
                        <buildArg>--initialize-at-run-time=io.netty.channel.epoll</buildArg>
                        <buildArg>--initialize-at-run-time=io.netty.channel.unix</buildArg>
                        <buildArg>--initialize-at-run-time=io.netty.channel.socket</buildArg>
                        <buildArg>--initialize-at-run-time=io.netty.channel.nio</buildArg>
                        <buildArg>--initialize-at-run-time=io.netty.handler.codec.compression</buildArg>
                        <buildArg>--initialize-at-run-time=io.netty.handler.ssl</buildArg>
                        <buildArg>--initialize-at-run-time=io.netty.buffer</buildArg>
                        <buildArg>--initialize-at-run-time=io.netty.util.internal</buildArg>
                        <buildArg>--initialize-at-run-time=io.netty.resolver</buildArg>
                    </buildArgs>
                </configuration>
                <executions>
                    <execution>
                        <id>add-reachability-metadata</id>
                        <goals>
                            <goal>add-reachability-metadata</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- SonarQube Maven Plugin -->
            <plugin>
                <groupId>org.sonarsource.scanner.maven</groupId>
                <artifactId>sonar-maven-plugin</artifactId>
                <version>4.0.0.4121</version>
            </plugin>
        </plugins>
    </build>

    <!-- ========================================= -->
    <!-- PROFILES: JVM vs Native                   -->
    <!-- ========================================= -->
    <profiles>
        <!-- Profile 1: JVM Tradicional (padrão) -->
        <profile>
            <id>jvm</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <spring-boot.build-image.skip>true</spring-boot.build-image.skip>
            </properties>
            <build>
                <finalName>beauty-salon-reactive-jvm</finalName>
                <plugins>
                    <plugin>
                        <groupId>org.springframework.boot</groupId>
                        <artifactId>spring-boot-maven-plugin</artifactId>
                        <configuration>
                            <mainClass>com.beautysalon.reactive.BeautySalonReactiveApplication</mainClass>
                            <classifier>jvm</classifier>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Profile 2: GraalVM Native Image -->
        <profile>
            <id>native</id>
            <properties>
                <spring-boot.build-image.skip>false</spring-boot.build-image.skip>
            </properties>
            <build>
                <finalName>beauty-salon-reactive-native</finalName>
                <plugins>
                    <plugin>
                        <groupId>org.graalvm.buildtools</groupId>
                        <artifactId>native-maven-plugin</artifactId>
                        <version>${native-buildtools.version}</version>
                        <extensions>true</extensions>
                        <executions>
                            <execution>
                                <id>build-native</id>
                                <goals>
                                    <goal>compile-no-fork</goal>
                                </goals>
                                <phase>package</phase>
                            </execution>
                        </executions>
                        <configuration>
                            <imageName>beauty-salon-reactive</imageName>
                            <mainClass>com.beautysalon.reactive.BeautySalonReactiveApplication</mainClass>
                            <buildArgs>
                                <buildArg>--no-fallback</buildArg>
                                <buildArg>--install-exit-handlers</buildArg>
                                <buildArg>--enable-url-protocols=http,https</buildArg>
                                <buildArg>-H:+ReportExceptionStackTraces</buildArg>
                                <buildArg>-H:+AddAllCharsets</buildArg>
                                <buildArg>--initialize-at-run-time=io.netty</buildArg>
                                <buildArg>--initialize-at-run-time=reactor.core.scheduler</buildArg>
                                <buildArg>--initialize-at-run-time=reactor.netty</buildArg>
                                <buildArg>--initialize-at-run-time=ch.qos.logback</buildArg>
                                <buildArg>--initialize-at-run-time=org.slf4j</buildArg>
                                <buildArg>--initialize-at-run-time=io.netty.util.internal.shaded.org.jctools</buildArg>
                                <buildArg>--allow-incomplete-classpath</buildArg>
                                <buildArg>--report-unsupported-elements-at-runtime</buildArg>
                                <buildArg>-H:ReflectionConfigurationFiles=${project.basedir}/src/main/resources/META-INF/native-image/reflect-config.json</buildArg>
                                <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.os</buildArg>
                                <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.util.concurrent</buildArg>
                                <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.metrics</buildArg>
                                <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.session</buildArg>
                                <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.channel</buildArg>
                                <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.connection</buildArg>
                                <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.pool</buildArg>
                                <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.cql</buildArg>
                                <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.metadata</buildArg>
                                <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.loadbalancing</buildArg>
                                <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.control</buildArg>
                                <buildArg>--initialize-at-run-time=com.datastax.oss.driver.internal.core.type</buildArg>
                                <buildArg>--initialize-at-run-time=com.datastax.oss.driver.api.core</buildArg>
                                <buildArg>--initialize-at-run-time=com.datastax.dse</buildArg>
                                <buildArg>--initialize-at-run-time=io.netty.channel.epoll</buildArg>
                                <buildArg>--initialize-at-run-time=io.netty.channel.unix</buildArg>
                                <buildArg>--initialize-at-run-time=io.netty.channel.socket</buildArg>
                                <buildArg>--initialize-at-run-time=io.netty.channel.nio</buildArg>
                                <buildArg>--initialize-at-run-time=io.netty.handler.codec.compression</buildArg>
                                <buildArg>--initialize-at-run-time=io.netty.handler.ssl</buildArg>
                                <buildArg>--initialize-at-run-time=io.netty.buffer</buildArg>
                                <buildArg>--initialize-at-run-time=io.netty.util.internal</buildArg>
                                <buildArg>--initialize-at-run-time=io.netty.resolver</buildArg>
                            </buildArgs>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
